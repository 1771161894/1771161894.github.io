---
title: "中国剩余定理非互质版"
date: 2026-08-06 00:12:36.000 +0000
last_modified_at: 2026-08-06 00:39:12.000 +0000
categories: ["工具箱", "代码模板"]
tags: ["C++", "数学", "数论"]
description: "逐步合并模数不一定互质的同余方程组。"
pin: false
math: false
mermaid: false
legacy_id: 42
---

> 逐步合并模数不一定互质的同余方程组。

## 模板作用

逐步合并模数不一定互质的同余方程组。

## 适用场景

多个余数条件同时成立，且模数可能不互质时使用。

## 复杂度

每次合并约 O(log M)。

## 使用提醒

先检查两个余数之差能否被 gcd 整除；合并后的模数是两模数的最小公倍数。

## 代码

````cpp
#include<bits/stdc++.h>

using namespace std;

typedef long long LL;

const int N = 30;

LL exgcd(LL a,LL b,LL &x,LL &y){
    if(!b){
        x = 1, y = 0;
        return a;
    }
    LL d = exgcd(b,a%b,y,x);
    y -= a/b * x;
    return d;
}

int main(){
    int n;
    scanf("%d",&n);
    LL a1,m1;
    bool flag = true;
    scanf("%lld%lld",&a1,&m1);
    for(int i = 0 ; i < n - 1 ; i++){
        LL a2,m2;
        scanf("%lld%lld",&a2,&m2);
        LL k1,k2;
        LL d = exgcd(a1,a2,k1,k2);
        if((m2 - m1) % d){
            flag = false;
            break;
        }
        LL t = (m2 - m1) / d;
        k1 *= t;
        k1 = (k1 % (a2/d) + (a2/d)) % (a2/d);
        m1 += a1 * k1;
        a1 = abs(a2/d * a1);
    }
    if(flag) printf("%lld",(m1 % a1 + a1) % a1);
    else puts("-1");
    return 0;
}
````

---

_本文由旧版个人知识库自动迁移，后续可直接在 GitHub 中继续修改。_
